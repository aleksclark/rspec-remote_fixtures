# frozen_string_literal: true

RSpec.describe Rspec::Remote::Fixtures do
  it "has a version number" do
    expect(Rspec::Remote::Fixtures::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
